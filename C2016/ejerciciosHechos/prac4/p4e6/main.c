#include "fechas.h"
#include <stdio.h>

int main(int argc, char const *argv[]){
	int aux;
	t_fecha* minac;

	minac = ini(minac);
	
	printf("%d %s %d %d\n",minac->dia,minac->mes,minac->anio,aux);
	return 0;
}